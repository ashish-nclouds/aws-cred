name: 'AWS Assume Role Action'
author: 'Aaron Batilo'
description: 'An action to use IAM user credentials to assume a role through STS'

outputs:
  aws_access_key_id:
  description: 'test'

runs:
  using: 'docker'
  image: 'Dockerfile'

branding:
  icon: 'users'
  color: 'orange'
